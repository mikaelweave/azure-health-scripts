# Create Import Body From Container

Creates a FHIR Service `$import` request body from the `*,ndjson` files in an Azure Storage container.

```
dotnet build
./bin/Debug/net8.0/CreateImportFromContainer
```